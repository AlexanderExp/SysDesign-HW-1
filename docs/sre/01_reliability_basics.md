# 01. Основы надёжности систем

> **Сложность:** ⭐ Базовый уровень
> **Вопросы из списка:** 1, 2, 5, 6, 8

---

## Вопрос 1: Что такое надёжность и что мы можем измерить на практике?

### Простыми словами

**Надёжность (Reliability)** — это способность системы работать правильно, даже когда что-то идёт не так.

Представь автомобиль:
- Надёжный автомобиль заводится каждое утро
- Даже если одна фара перегорит, он всё равно едет
- Если закончился бензин — он предупредит заранее

### Формальное определение

Система надёжна, если она:
1. **Выполняет свои функции** — делает то, что должна
2. **Толерантна к ошибкам** — продолжает работать при сбоях
3. **Предотвращает несанкционированный доступ** — защищена

### Что измеряем на практике?

#### SLI (Service Level Indicator) — индикаторы уровня сервиса

Это **конкретные метрики**, которые можно измерить:

| Метрика | Что измеряет | Пример |
|---------|--------------|--------|
| **Availability** | Доступность | 99.9% времени сервис отвечает |
| **Latency** | Задержка | 95% запросов < 200ms |
| **Throughput** | Пропускная способность | 1000 запросов/сек |
| **Error Rate** | Частота ошибок | < 0.1% ошибок 5xx |

#### SLO (Service Level Objective) — цели

**SLO** — это целевые значения SLI:
- "Доступность должна быть ≥ 99.9%"
- "Задержка p99 должна быть ≤ 500ms"

#### SLA (Service Level Agreement) — соглашение

**SLA** — это юридический договор с клиентом:
- "Если доступность < 99.9%, мы вернём деньги"

### Формула доступности

```
Availability = Uptime / (Uptime + Downtime) × 100%
```

**Таблица "девяток":**

| Доступность | Downtime в год | Downtime в месяц |
|-------------|----------------|------------------|
| 99% (две девятки) | 3.65 дня | 7.3 часа |
| 99.9% (три девятки) | 8.76 часа | 43.8 минуты |
| 99.99% (четыре девятки) | 52.6 минуты | 4.38 минуты |
| 99.999% (пять девяток) | 5.26 минуты | 26.3 секунды |

### Пример из жизни

```
Сервис работал:
- Январь: 30 дней, из них 2 часа был недоступен
- Availability = (30*24 - 2) / (30*24) = 718/720 = 99.72%
```

### Error Budget (бюджет ошибок)

**Error Budget** — это допустимое количество "плохого" времени, которое можно потратить.

```
┌─────────────────────────────────────────────────────────┐
│                    ERROR BUDGET                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  SLO: 99.9% доступность                                 │
│  Error Budget = 100% - 99.9% = 0.1%                     │
│                                                         │
│  В месяц (30 дней):                                     │
│  30 × 24 × 60 = 43,200 минут                            │
│  Error Budget = 43,200 × 0.1% = 43.2 минуты             │
│                                                         │
│  Использование бюджета:                                 │
│  ┌──────────────────────────────────────────────┐       │
│  │ Инцидент 1: 15 мин     ████████               │       │
│  │ Инцидент 2: 10 мин     █████                  │       │
│  │ Инцидент 3: 8 мин      ████                   │       │
│  │ ─────────────────────────────────────────     │       │
│  │ Потрачено: 33 мин      Осталось: 10.2 мин     │       │
│  └──────────────────────────────────────────────┘       │
│                                                         │
│  Правило:                                               │
│  • Бюджет есть → можно делать рискованные релизы        │
│  • Бюджет исчерпан → замораживаем изменения,            │
│    фокусируемся на надёжности                           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Как правильно измерять SLI

```
┌─────────────────────────────────────────────────────────┐
│                  ИЗМЕРЕНИЕ SLI                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. AVAILABILITY (доступность)                          │
│     ─────────────────────────                           │
│     Формула: успешные запросы / все запросы             │
│                                                         │
│     ❌ Плохо: Проверять только "сервер отвечает"        │
│     ✅ Хорошо: Проверять "запрос выполнен успешно"      │
│                                                         │
│     SLI = (total_requests - 5xx_errors) / total_requests│
│                                                         │
│  2. LATENCY (задержка)                                  │
│     ─────────────────                                   │
│     Используй перцентили, не среднее!                   │
│                                                         │
│     ❌ Плохо: avg(latency) = 100ms                      │
│        (скрывает выбросы: 99 запросов по 10ms,          │
│         1 запрос 9 секунд → avg = 100ms)                │
│                                                         │
│     ✅ Хорошо:                                          │
│        p50 = 50ms   (медиана)                           │
│        p95 = 200ms  (95% запросов быстрее)              │
│        p99 = 500ms  (99% запросов быстрее)              │
│                                                         │
│  3. THROUGHPUT (пропускная способность)                 │
│     ───────────────────────────────────                 │
│     Запросов в секунду (RPS) или транзакций (TPS)       │
│                                                         │
│     SLI = rate(requests_total[5m])                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Связь SLI → SLO → SLA

```
┌─────────────────────────────────────────────────────────┐
│                   SLI → SLO → SLA                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  SLI (что измеряем):                                    │
│  "Процент успешных запросов за последние 30 дней"       │
│                                                         │
│           ▼                                             │
│                                                         │
│  SLO (наша внутренняя цель):                            │
│  "99.9% запросов должны быть успешными"                 │
│  "p99 latency ≤ 300ms"                                  │
│                                                         │
│           ▼                                             │
│                                                         │
│  SLA (договор с клиентом):                              │
│  "Гарантируем 99.5% доступность"                        │
│  "При нарушении — компенсация 10% от счёта"             │
│                                                         │
│  ⚠️ SLA всегда слабее SLO!                              │
│     SLO = 99.9%, SLA = 99.5%                            │
│     Запас на случай проблем                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## Вопрос 2: Из чего состоит надёжность? Что влияет на надёжность?

### Три кита надёжности

```
                    НАДЁЖНОСТЬ
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
   ДОСТУПНОСТЬ     КОРРЕКТНОСТЬ    БЕЗОПАСНОСТЬ
   (Availability)  (Correctness)   (Security)
```

### 1. Доступность (Availability)

**Что это:** Система отвечает на запросы.

**Что влияет:**
- Отказы оборудования (диски, сеть, память)
- Баги в коде
- Перегрузка (слишком много запросов)
- Зависимости (упала база — упал сервис)

**Как улучшить:**
- Резервирование (несколько серверов)
- Балансировка нагрузки
- Graceful degradation (частичная работа при сбоях)

### 2. Корректность (Correctness)

**Что это:** Система возвращает правильные результаты.

**Что влияет:**
- Баги в бизнес-логике
- Гонки данных (race conditions)
- Потеря данных
- Несогласованность данных

**Как улучшить:**
- Тестирование (unit, integration, e2e)
- Транзакции в базе данных
- Идемпотентность операций

### 3. Безопасность (Security)

**Что это:** Система защищена от несанкционированного доступа.

**Что влияет:**
- Уязвимости в коде
- Слабые пароли
- Отсутствие шифрования
- SQL-инъекции, XSS

**Как улучшить:**
- Аутентификация и авторизация
- Шифрование (TLS, at-rest encryption)
- Аудит и логирование

### Факторы, влияющие на надёжность

```
┌─────────────────────────────────────────────────────────┐
│                    ВНЕШНИЕ ФАКТОРЫ                      │
├─────────────────────────────────────────────────────────┤
│ • Нагрузка (пики трафика, DDoS)                        │
│ • Сетевые проблемы (потеря пакетов, задержки)          │
│ • Отказы инфраструктуры (AWS падает, диск умер)        │
│ • Человеческий фактор (неправильный деплой)            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   ВНУТРЕННИЕ ФАКТОРЫ                    │
├─────────────────────────────────────────────────────────┤
│ • Качество кода (баги, утечки памяти)                  │
│ • Архитектура (single point of failure)                │
│ • Зависимости (библиотеки, внешние API)                │
│ • Конфигурация (неправильные настройки)                │
└─────────────────────────────────────────────────────────┘
```

---

## Вопрос 5: На какие вопросы про зависимости надо ответить при проектировании?

### Чек-лист вопросов о зависимостях

При проектировании системы для каждой зависимости спроси себя:

#### 1. Критичность
```
❓ Что будет, если эта зависимость недоступна?
   □ Сервис полностью упадёт
   □ Часть функций не будет работать
   □ Ничего страшного, есть fallback
```

#### 2. Надёжность зависимости
```
❓ Какой SLA у этой зависимости?
   □ 99.99% — очень надёжная
   □ 99.9% — нормальная
   □ 99% — ненадёжная, нужен план Б
   □ Неизвестно — ОПАСНО!
```

#### 3. Время ответа
```
❓ Как быстро отвечает зависимость?
   □ < 10ms — быстро
   □ 10-100ms — нормально
   □ > 100ms — медленно, нужен кэш или async
```

#### 4. Поведение при сбое
```
❓ Как зависимость ведёт себя при перегрузке?
   □ Возвращает ошибку быстро — хорошо
   □ Висит долго (timeout) — плохо
   □ Падает полностью — очень плохо
```

#### 5. Стратегия отказоустойчивости
```
❓ Что делать при недоступности?
   □ Retry (повторить)
   □ Fallback (запасной вариант)
   □ Circuit Breaker (отключить на время)
   □ Graceful Degradation (работать без этой функции)
```

### Пример анализа зависимостей

```
Наш сервис: Rental Service
                │
                ├── PostgreSQL (БД)
                │   ├── Критичность: ВЫСОКАЯ (без БД не работаем)
                │   ├── SLA: 99.99%
                │   ├── Latency: 5ms
                │   └── Стратегия: Реплика для чтения, retry для записи
                │
                ├── Payment Gateway (внешний API)
                │   ├── Критичность: СРЕДНЯЯ (можно отложить платёж)
                │   ├── SLA: 99.9%
                │   ├── Latency: 200-500ms
                │   └── Стратегия: Circuit Breaker + очередь
                │
                └── Redis (кэш)
                    ├── Критичность: НИЗКАЯ (можно без кэша)
                    ├── SLA: 99.9%
                    ├── Latency: 1ms
                    └── Стратегия: Fallback на БД
```

---

## Вопрос 6: Что может исчерпаться в коде и привести к сбою?

### Ресурсы, которые заканчиваются

#### 1. Память (RAM)

```
Проблемы:
• Утечки памяти (memory leaks)
• Слишком большие объекты в памяти
• Кэш без лимита

Симптомы:
• OOM Killer убивает процесс
• Swap thrashing (система тормозит)
• OutOfMemoryError

Решения:
✓ Мониторинг потребления памяти
✓ Лимиты на кэши (LRU eviction)
✓ Профилирование памяти
```

#### 2. CPU

```
Проблемы:
• Бесконечные циклы
• Неоптимальные алгоритмы O(n²)
• Слишком много потоков

Симптомы:
• 100% CPU
• Высокий load average
• Таймауты запросов

Решения:
✓ Профилирование CPU
✓ Rate limiting
✓ Оптимизация алгоритмов
```

#### 3. Файловые дескрипторы

```
Проблемы:
• Незакрытые соединения
• Незакрытые файлы
• Слишком много открытых сокетов

Симптомы:
• "Too many open files"
• Невозможно открыть новые соединения

Решения:
✓ Закрывать ресурсы (try-with-resources, defer)
✓ Connection pooling
✓ Увеличить лимит ulimit
```

#### 4. Соединения с БД (Connection Pool)

```
Проблемы:
• Пул исчерпан
• Соединения не возвращаются в пул
• Медленные запросы держат соединения

Симптомы:
• "Connection pool exhausted"
• Таймауты при получении соединения

Решения:
✓ Правильный размер пула
✓ Таймауты на соединения
✓ Мониторинг пула
```

#### 5. Дисковое пространство

```
Проблемы:
• Логи растут бесконечно
• Временные файлы не удаляются
• База данных растёт

Симптомы:
• "No space left on device"
• БД не может записать данные

Решения:
✓ Ротация логов (logrotate)
✓ Очистка временных файлов
✓ Мониторинг диска
```

#### 6. Сетевые порты

```
Проблемы:
• Исчерпание ephemeral ports
• TIME_WAIT накопление

Симптомы:
• "Cannot assign requested address"
• Новые соединения не создаются

Решения:
✓ Connection pooling
✓ Keep-alive соединения
✓ Настройка tcp_tw_reuse
```

#### 7. Потоки (Threads)

```
Проблемы:
• Thread pool исчерпан
• Deadlocks
• Thread starvation

Симптомы:
• Запросы висят в очереди
• Система не отвечает

Решения:
✓ Bounded thread pools
✓ Async/non-blocking IO
✓ Мониторинг потоков
```

### Сводная таблица

| Ресурс | Как мониторить | Что делать |
|--------|----------------|------------|
| RAM | `free -m`, метрики | Лимиты, профилирование |
| CPU | `top`, `htop` | Rate limiting, оптимизация |
| File descriptors | `lsof`, `ulimit -n` | Закрывать ресурсы |
| DB connections | Метрики пула | Правильный размер пула |
| Disk | `df -h` | Ротация, очистка |
| Network ports | `netstat`, `ss` | Connection pooling |
| Threads | Thread dump | Bounded pools |

---

## Вопрос 8: О чём надо подумать при проектировании с точки зрения надёжности?

### Чек-лист архитектора (без деталей)

#### 🔴 Критические вопросы

```
□ Single Point of Failure (SPOF)
  Есть ли компоненты, отказ которых убьёт всю систему?

□ Зависимости
  Что будет, если зависимость недоступна?

□ Масштабирование
  Как система справится с 10x нагрузкой?

□ Данные
  Как защищены данные? Есть ли бэкапы?
```

#### 🟡 Важные вопросы

```
□ Деградация
  Может ли система работать частично при сбоях?

□ Таймауты
  Установлены ли таймауты на все внешние вызовы?

□ Retry логика
  Есть ли повторные попытки? С backoff?

□ Circuit Breaker
  Защищена ли система от каскадных сбоев?

□ Rate Limiting
  Защищена ли система от перегрузки?
```

#### 🟢 Операционные вопросы

```
□ Мониторинг
  Как узнаем о проблемах?

□ Алертинг
  Кто и когда получит уведомление?

□ Логирование
  Достаточно ли логов для диагностики?

□ Откат
  Можно ли откатить изменения?

□ Документация
  Есть ли runbook для инцидентов?
```

### Визуальный чек-лист

```
┌─────────────────────────────────────────────────────────┐
│              ПРОЕКТИРОВАНИЕ НАДЁЖНОСТИ                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  АРХИТЕКТУРА           ЗАЩИТА              ОПЕРАЦИИ     │
│  ────────────          ──────              ─────────    │
│  □ Нет SPOF            □ Таймауты          □ Мониторинг │
│  □ Резервирование      □ Retry             □ Алертинг   │
│  □ Масштабирование     □ Circuit Breaker   □ Логи       │
│  □ Изоляция            □ Rate Limiting     □ Откаты     │
│  □ Кэширование         □ Валидация         □ Runbooks   │
│                                                         │
│  ДАННЫЕ                ТЕСТИРОВАНИЕ                     │
│  ──────                ────────────                     │
│  □ Бэкапы              □ Unit тесты                     │
│  □ Репликация          □ Integration тесты              │
│  □ Консистентность     □ Load тесты                     │
│  □ Шифрование          □ Chaos engineering              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## Ключевые термины

| Термин | Определение |
|--------|-------------|
| **SLI** | Service Level Indicator — измеримая метрика (latency, availability) |
| **SLO** | Service Level Objective — целевое значение SLI |
| **SLA** | Service Level Agreement — договор с клиентом |
| **SPOF** | Single Point of Failure — единая точка отказа |
| **Availability** | Доступность — процент времени, когда система работает |
| **Latency** | Задержка — время ответа на запрос |
| **Throughput** | Пропускная способность — количество запросов в секунду |
| **Graceful Degradation** | Частичная работа системы при сбоях |

---

## Что запомнить

1. **Надёжность = Доступность + Корректность + Безопасность**
2. **Измеряем через SLI → ставим цели SLO → договариваемся SLA**
3. **Всё, что может закончиться — закончится** (память, соединения, диск)
4. **Для каждой зависимости нужен план Б**
5. **Проектируй с мыслью "а что если это сломается?"**

---

*Следующий файл: [02. CAP-теорема и распределённый консенсус](02_cap_consensus.md)*

