[project]
name = "shared"
version = "0.1.0"
description = "Shared database module for rental system"
requires-python = ">=3.11,<3.12"
dependencies = [
    "sqlalchemy>=2.0.0",
    "loguru>=0.7.0",
    "python-dotenv>=1.0.0",
]
