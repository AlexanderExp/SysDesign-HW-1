# 🎨 Демо UI для системы аренды пауэрбанков

Простой веб-интерфейс для демонстрации работы системы на защите.

---

## 🚀 Быстрый старт

### 1. Запустите систему

```bash
# Из корня проекта
docker compose up -d
```

### 2. Откройте UI

Просто откройте файл `index.html` в браузере:

```bash
# macOS
open demo-ui/index.html

# Linux
xdg-open demo-ui/index.html

# Windows
start demo-ui/index.html
```

Или через Python HTTP сервер:

```bash
cd demo-ui
python3 -m http.server 8080
```

Затем откройте: http://localhost:8080

---

## 📋 Возможности

### 1️⃣ Создание оффера
- Выбор пользователя и станции
- Получение цены, депозита, бесплатного периода
- Автоматическое заполнение Quote ID для следующего шага

### 2️⃣ Старт аренды
- Использование полученного оффера
- Автогенерация Idempotency-Key
- Получение Order ID и Powerbank ID

### 3️⃣ Статус аренды
- Просмотр текущего статуса
- Отображение накопленной суммы и долга
- **Авто-обновление каждые 3 секунды** (для демонстрации работы billing-worker)

### 4️⃣ Завершение аренды
- Выбор станции для возврата
- Получение итоговой суммы

### 📋 Логи
- Все HTTP запросы и ответы
- Цветовая индикация (запрос/ответ/ошибка)
- Форматированный JSON
- Автоматическая прокрутка

---

## 🎯 Сценарии для демонстрации

### Сценарий 1: Успешная аренда

1. **Создайте оффер**
   - User ID: `user-demo-001`
   - Station ID: `some-station-id`
   - Нажмите "Создать оффер"
   
2. **Начните аренду**
   - Quote ID уже заполнен
   - Нажмите "Начать аренду"
   
3. **Включите авто-обновление статуса**
   - Нажмите "🔄 Авто-обновление"
   - Наблюдайте, как растет `total_amount` каждые 3 секунды
   - Это демонстрирует работу billing-worker!
   
4. **Завершите аренду**
   - Выберите станцию возврата
   - Нажмите "Завершить аренду"

### Сценарий 2: Пустая станция (ошибка)

1. **Создайте оффер**
   - Station ID: `empty-station-id`
   - Нажмите "Создать оффер"
   
2. **Попробуйте начать аренду**
   - Получите ошибку 409 "no free slots"
   - Демонстрирует обработку ошибок

### Сценарий 3: Идемпотентность

1. **Создайте оффер и начните аренду**
2. **НЕ МЕНЯЙТЕ Idempotency-Key**
3. **Нажмите "Начать аренду" еще раз**
   - Получите тот же Order ID
   - Демонстрирует идемпотентность!

### Сценарий 4: Протухший оффер

1. **Создайте оффер**
2. **Подождите 60+ секунд** (expires_in_sec)
3. **Попробуйте начать аренду**
   - Получите ошибку 400 "invalid or expired quote"
   - Демонстрирует контроль свежести оффера!

---

## 🎨 Особенности UI

### Дизайн
- 🎨 Современный gradient фон
- 📱 Адаптивная верстка
- ✨ Плавные анимации
- 🎯 Интуитивный интерфейс

### Логирование
- 🔵 Синий - запросы
- 🟢 Зеленый - успешные ответы
- 🔴 Красный - ошибки
- ⏱️ Временные метки
- 📊 Форматированный JSON

### Автоматизация
- ✅ Автозаполнение полей
- ✅ Автогенерация UUID
- ✅ Авто-обновление статуса
- ✅ Валидация состояний

---

## 🔧 Настройка

### Изменить URL API

Отредактируйте в `index.html`:

```javascript
const API_BASE = 'http://localhost:8000';  // Измените на нужный
```

### Изменить интервал авто-обновления

```javascript
autoRefreshInterval = setInterval(() => {
    getStatus();
}, 3000);  // Измените на нужный (в миллисекундах)
```

---

## 📸 Скриншоты для презентации

### Как сделать скриншоты:

1. Откройте UI в браузере
2. Выполните полный цикл аренды
3. Сделайте скриншот с логами
4. Вставьте в презентацию

### Что показать:

- ✅ Успешное создание оффера
- ✅ Старт аренды с Idempotency-Key
- ✅ Авто-обновление статуса (растущая сумма)
- ✅ Логи всех запросов/ответов
- ✅ Обработка ошибок

---

## 🎤 Что говорить на защите

### При демонстрации UI:

> "Для удобства демонстрации мы создали веб-интерфейс, который показывает работу всех основных ручек API."

### При создании оффера:

> "Здесь мы видим, как система рассчитывает цену на основе тарифа, профиля пользователя и текущей конфигурации. Оффер живет 60 секунд."

### При старте аренды:

> "Обратите внимание на Idempotency-Key - если повторить запрос с тем же ключом, мы получим тот же Order ID. Это защита от дублирования."

### При авто-обновлении:

> "Включаем авто-обновление статуса каждые 3 секунды. Видите, как растет total_amount? Это работает billing-worker - он каждые 5 секунд пересчитывает начисления и списывает деньги."

### При просмотре логов:

> "В логах видны все HTTP запросы и ответы. Синим - запросы, зеленым - успешные ответы. Все данные в формате JSON для удобства отладки."

---

## 🐛 Troubleshooting

### UI не подключается к API

**Проблема**: CORS ошибка в консоли браузера

**Решение**: Добавьте CORS в rental-core:

```python
# services/rental-core/app/main.py
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # В продакшене указать конкретные домены
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### API возвращает 404

**Проблема**: rental-core не запущен

**Решение**:
```bash
docker compose up -d rental-core
docker compose logs -f rental-core
```

### Офферы сразу протухают

**Проблема**: Офферы в PostgreSQL вместо Redis (см. рекомендации)

**Решение**: Переключите на Redis (см. ARCHITECTURE_DIAGRAMS.md)

---

## 📦 Файлы

```
demo-ui/
├── index.html          # Основной файл UI
└── README.md          # Эта документация
```

---

## 🎯 Чеклист для защиты

- [ ] Система запущена (`docker compose up -d`)
- [ ] UI открывается в браузере
- [ ] API доступен (зеленый лог health check)
- [ ] Создание оффера работает
- [ ] Старт аренды работает
- [ ] Авто-обновление показывает рост суммы
- [ ] Логи отображаются корректно
- [ ] Завершение аренды работает
- [ ] Скриншоты готовы для презентации

---

**Готово к демонстрации! 🚀**

